export type ShortTagType = {
  tagKey: string;
  tagValue: string;
  tagColor: string;
};

export type TagType = {
  id: string;
  tagKey: string;
  tagValue: string;
  tagColor: string;
};

export type CreateTagType = {
  tagKey: string;
  tagValue: string;
  tagColor: string;
  errorId: string;
};
